langchain==0.1.20
langchain-openai==0.1.7
langchain-core==0.1.52
python-dotenv
# Only for standard_agent.py / sbp_agent.py, which still use the deprecated
# create_openai_functions_agent()+AgentExecutor API. complex_demo.py/sbp_worker.py need
# requirements.txt instead (langchain>=1.0.0) -- create_openai_functions_agent doesn't
# exist there, so these two dependency sets cannot be merged into one file.
# Install sbp-client from local package:
# pip install -e ../../../packages/client-python